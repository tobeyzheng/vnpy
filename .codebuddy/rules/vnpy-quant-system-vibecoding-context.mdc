---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-05-07T23:26:54.207Z
provider: 
---

# vnpy 量化交易系统 Vibe Coding 上下文

## 当前主线边界
- 以 `scripts/`、`services/`、`execution/`、`state/runs/`、`docs/system_integration_guide.md` 为当前主线。
- 不再以 `examples/futu_trader/` 作为开发基线；examples 仅作历史参考。
- 不要删除 `.codebuddy/`，它是项目数据目录，不是缓存。

## 主入口
- 本地模拟盘：`scripts/run_hk_sim_task.py`、`scripts/run_us_sim_task.py`。
- 本地模拟平仓：`scripts/run_hk_sim_close.py`、`scripts/run_us_sim_close.py`。
- Futu SIM 会话：`scripts/run_hk_futu_sim_session.py`、`scripts/run_us_futu_sim_session.py`。
- 实盘 dry-run/live 门控：`scripts/run_hk_live_task.py`、`scripts/run_us_live_task.py`。
- 健康检查与组合简报：`scripts/run_healthcheck.py`、`scripts/run_portfolio_brief.py`。

## 核心模块
- 候选池：`services/strategy/candidate_provider.py`。
- 策略统一入口：`services/strategy/engine.py`。
- 策略选择：`services/strategy/strategy_selector.py`、`services/strategy/external_selection.py`。
- 模拟 pipeline：`services/trading_pipeline/sim_task.py`、`services/trading_pipeline/close_task.py`。
- 实盘 pipeline：`services/trading_pipeline/live_task.py`。
- Futu 模拟提交：`services/futu_sim_trade/client.py`。
- 订单状态：`services/trade_state/state_machine.py`、`services/trade_state/storage.py`。
- 风控/对账：`services/execution_guard/`、`services/risk_engine/`、`services/portfolio/risk.py`。
- Knot/LLM：`services/knot_runtime/`、`services/evaluation_hub/`。

## 安全边界
- 默认不做真实实盘提交。
- 真实提交需显式开关，至少包括 `--live-submit`、`VNPY_LIVE_CONFIG=YES`、`VNPY_LIVE_SUBMIT=YES`；默认还要求 `VNPY_LIVE_APPROVED=YES`。
- Knot/LLM 只提供结构化评估或策略建议，不能直接决定下单数量，不能绕过风控、对账、审批和订单幂等。
- Futu SIM 会话只能操作 `TrdEnv.SIMULATE`，不得切到 REAL。

## 状态文件原则
- `state/runs/candidate_inputs*.json`、账户文件、最新 task/session report、`orders/`、`strategy_selection/` 属于主流程产物，默认保留。
- `state/runs/*demo*.json`、临时验证结果、过期日志、pid 文件可以清理。
- 清理前优先确认文件不是当前运行中的进程产物。

## 代码修改原则
- 优先复用 `services/` 中的业务模块，`scripts/` 只做入口和参数配置。
- 新增交易能力必须写报告、写订单状态、支持 dry-run/显式开关，并处理 Futu 超时重试。
- 不主动创建新的 Markdown 文档；如需文档，优先更新已有 `docs/system_integration_guide.md`。