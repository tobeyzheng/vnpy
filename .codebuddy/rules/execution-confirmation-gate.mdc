---
description: 所有实际执行动作必须先获得用户确认
alwaysApply: true
enabled: true
updatedAt: 2026-05-08T17:55:00+08:00
provider: 
---

# 实际执行前确认规则

## 总原则

在本项目中，凡是可能产生真实运行效果、交易效果、账户状态变化、远端状态变化或重要本地产物变化的动作，执行前必须先向用户说明影响范围，并等待用户明确确认。

## 必须先确认的动作

包括但不限于：

- 启动、停止或重启任何交易会话、定时会话、后台进程。
- 运行 Futu SIM 下单、撤单、调仓、平仓脚本。
- 运行 Futu REAL / live 相关脚本，即使当前有硬开关保护。
- 运行会改写账户、持仓、订单、对账、报告或 `state/runs/` 关键状态的脚本。
- 运行回测、批量数据导入、批量候选生成、KnotAgent 批处理、自动化任务。
- 安装依赖、修改环境变量、启动服务、连接 OpenD 或远端服务的执行动作。

## 可直接进行的动作

以下动作默认允许直接进行：

- 阅读、搜索、分析代码和日志。
- 修改代码、配置或文档以实现用户明确提出的开发需求。
- 运行不产生交易、不改写关键状态的轻量语法检查，例如 `py_compile`、linter。
- 对已读取日志、报告、回测结果做解释分析。

## 确认话术要求

执行前需要说明：

- 要执行的命令或脚本。
- 是否会连接 Futu/OpenD/远端服务。
- 是否会提交 SIM/REAL 订单。
- 是否会改写 `state/runs/`、账户文件、订单状态或远端 Git。
- 预计输出文件。

只有用户明确回复类似 `确认执行`、`确认提交推送`、`确认启动` 后，才可以执行对应动作。

## 交易安全补充

- 默认不允许真实 Futu 账户交易。
- Futu REAL/live 相关执行必须额外确认，并满足项目硬开关、人工审批、对账、风控、订单幂等要求。
- KnotAgent/LLM 只能提供结构化建议、过滤或降仓，不能直接绕过风控生成真实订单。
