---
description: 计划状态、历史记录、敏感信息与文档同步规则
alwaysApply: true
enabled: true
updatedAt: 2026-05-10T11:10:00+08:00
provider:
---

# 计划状态、历史记录、敏感信息与文档同步规则

## Plan item 状态同步

- 已开始的 plan item、task item 或子任务，应在对应 markdown 中及时更新状态，不要长期停留在未开始状态。
- 已完成的 plan item、task item 或子任务，应在同一轮改动内及时更新为已完成，并补充必要的当前焦点或阶段说明。
- 若项目中存在 `.codebuddy/task_list/` 或其他计划 markdown，默认优先更新这些文件，保持记录可追踪。

## 历史操作文档记录

- 对框架、需求、入口、规则、运行产物结构、阶段边界有影响的改动，应同步记录到 `docs/project_operation_log.md`。
- 记录应包含日期、变更范围、关键文件和影响摘要，便于后续协作者快速回溯。
- 历史记录以高层摘要为主，不要求粘贴大段实现细节。

## 敏感信息最小暴露

- 在回复消息、说明文本、文档、日志、调试输出中，避免出现真实账户金额、完整账号、密钥、令牌等敏感信息。
- 如需表达资金约束、账户规模或预算信息，优先使用脱敏占位符、区间或定性描述。
- 如引用已有日志或文件内容，涉及敏感金额时必须先脱敏再输出。

## 文档同步要求

- 框架、需求、入口、默认参数、运行边界、产物路径等发生变化时，必须同步更新 `docs/` 下相关文档。
- 文档更新应与代码或规则改动在同一轮完成，不要把文档补充推迟到后续回合。
- 入口与集成链路变化，至少检查 `docs/system_integration_guide.md`；策略或架构变化，同时检查 `docs/adaptive_quant_engine_design.md`。
- 需要长期追溯的改动，除更新主题文档外，还应补充 `docs/project_operation_log.md` 记录。
